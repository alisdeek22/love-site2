<!doctype html>
<html lang="ar">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>رسائل حب</title>

  <!-- استخدم خط آبل / آيفون مع بدائل -->
  <style>
    :root{
      --bg:#0f0b1a;
      --accent:#ff6b9a;
      --accent-2:#ff9ac4;
      --glass: rgba(255,255,255,0.06);
    }
    /* system san-francisco fallbacks */
    @font-face {
      font-family: "SanFranciscoFallback";
      src: local("-apple-system");
    }
    html,body{height:100%}
    body{
      margin:0;
      font-family: -apple-system, "SanFranciscoFallback", "SF Pro Text", "SF Pro Display", "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: radial-gradient(ellipse at 20% 10%, rgba(255,107,154,0.06), transparent 10%),
                  linear-gradient(180deg, #0b0711 0%, #15051a 100%);
      color:#fff;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:20px;
      direction:rtl;
    }
    /* container */
    .card{
      width:100%;
      max-width:920px;
      border-radius:18px;
      padding:28px;
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      box-shadow: 0 10px 30px rgba(0,0,0,0.6);
      position:relative;
      overflow:hidden;
    }

    /* animated hearts background */
    .hearts{
      position:absolute;
      inset:0;
      pointer-events:none;
      z-index:0;
      opacity:0.18;
      mix-blend-mode:screen;
      background-image:
        radial-gradient(circle at 10% 20%, rgba(255,155,180,0.08) 0 2%, transparent 6%),
        radial-gradient(circle at 80% 30%, rgba(255,107,154,0.06) 0 2%, transparent 6%),
        radial-gradient(circle at 50% 80%, rgba(255,154,200,0.05) 0 2%, transparent 6%);
    }
    .hearts:after{
      content:"";
      position:absolute;
      inset:0;
      background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="400" height="400"><g fill="%23ff6b9a" opacity="0.12"><path d="M200 320s-86-56-120-112C52 152 120 88 200 128c80-40 148 24 120 80-34 56-120 112-120 112z"/></g></svg>');
      background-repeat:repeat;
      transform:scale(1.6);
      animation: float 12s linear infinite;
    }
    @keyframes float{
      0%{transform:translateY(0) scale(1.6)}
      50%{transform:translateY(-30px) scale(1.62)}
      100%{transform:translateY(0) scale(1.6)}
    }

    header{
      display:flex;
      align-items:center;
      gap:16px;
      z-index:3;
      position:relative;
    }
    .logo{
      width:64px;height:64px;border-radius:12px;background:linear-gradient(135deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;font-weight:700;font-size:22px;
      box-shadow:0 6px 20px rgba(255,107,154,0.14), inset 0 -6px 18px rgba(255,255,255,0.05);
    }
    h1{margin:0;font-size:22px}
    p.lead{margin:6px 0 0 0;color:rgba(255,255,255,0.85);font-size:14px}

    .content{
      margin-top:22px;
      display:grid;
      grid-template-columns:1fr 340px;
      gap:20px;
      z-index:3;
      position:relative;
    }

    /* left: messages area */
    .messages{
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius:12px;padding:18px;
      min-height:320px;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.02);
    }
    .buttons-row{
      display:flex;
      gap:8px;
      flex-wrap:wrap;
      margin-bottom:12px;
    }
    .btn{
      background:transparent;border:1px solid rgba(255,255,255,0.09);padding:10px 14px;border-radius:10px;cursor:pointer;font-weight:600;
      transition: all .18s ease;
    }
    .btn:hover{transform:translateY(-4px);box-shadow:0 6px 20px rgba(0,0,0,0.45)}
    .btn.primary{
      background:linear-gradient(90deg,var(--accent),var(--accent-2));
      color:#fff;border:none;
    }

    .message-box{
      margin-top:14px;background:rgba(0,0,0,0.28);padding:16px;border-radius:10px;min-height:130px;
      display:flex;align-items:center;justify-content:center;font-size:18px;line-height:1.4;text-align:center;
    }

    /* right: side card */
    .side{
      background: linear-gradient(180deg, rgba(255,255,255,0.015), rgba(255,255,255,0.01));
      border-radius:12px;padding:16px;
      display:flex;flex-direction:column;gap:12px;
      min-height:320px;
    }
    .side h3{margin:0;font-size:16px}
    .secret{
      background:var(--glass);padding:12px;border-radius:8px;min-height:88px;display:flex;align-items:center;justify-content:center;text-align:center;
    }

    /* form modal */
    .modal{
      position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(3,3,5,0.6);z-index:60;visibility:hidden;opacity:0;transition:all .18s ease;
    }
    .modal.open{visibility:visible;opacity:1}
    .modal-card{width:100%;max-width:520px;background:linear-gradient(180deg,#0d0710,#111);padding:18px;border-radius:12px;border:1px solid rgba(255,255,255,0.03)}
    .form-row{display:flex;flex-direction:column;gap:8px;margin-bottom:10px}
    input,textarea{background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.04);padding:10px;border-radius:8px;color:#fff;outline:none}
    textarea{min-height:100px;resize:vertical}
    .modal-actions{display:flex;gap:8px;justify-content:flex-end;margin-top:8px}

    /* responsive */
    @media (max-width:900px){
      .content{grid-template-columns:1fr}
      .side{order:2}
    }
    /* small touches */
    .small{font-size:13px;color:rgba(255,255,255,0.7)}
    .copy-btn{margin-top:8px;padding:8px;border-radius:8px;border:1px dashed rgba(255,255,255,0.06);background:transparent;cursor:pointer}
  </style>
  <link rel="icon" href="data:;base64,=">
</head>
<body>
  <div class="card" role="main">
    <div class="hearts" aria-hidden="true"></div>

    <header>
      <div class="logo">❤</div>
      <div>
        <h1>رسائل حب</h1>
        <p class="lead">اختار رسالة، عدّلها، وابعتها للغالي/ة</p>
      </div>
    </header>

    <div class="content">
      <div class="messages">
        <div class="buttons-row" aria-hidden="false">
          <button class="btn" data-msg="بحبك فوق ما تتخيل">بحبك فوق ما تتخيل</button>
          <button class="btn" data-msg="وحشتيني يا قلبي">وحشتني يا قلبي</button>
          <button class="btn" data-msg="انتي دايمًا في بالي">انتي دايمًا في بالي</button>
          <button class="btn primary" id="open-form">ابعت رسالة</button>
          <button class="btn" id="open-secret">رسالة سرية</button>
        </div>

        <div class="message-box" id="messageBox">اضغط على أي زر علشان تظهر الرسالة هنا 💌</div>
      </div>

      <aside class="side" aria-label="الخيارات">
        <h3>إعدادات سريعة</h3>
        <div class="small">ألوان وخطوط تقدر تعدّلهم في ملف <code>styles.css</code></div>

        <div>
          <h3>الرسالة السرية</h3>
          <div class="secret" id="secretBox">مفيش رسالة سرية لسه — اضغط "رسالة سرية".</div>
          <button class="copy-btn" id="copySecret">انسخ الرسالة السرية</button>
        </div>

        <div>
          <h3>مشاركة</h3>
          <div class="small">تقدر تشارك الصفحة أو تحملها وتبعتها.</div>
        </div>
      </aside>
    </div>
  </div>

  <!-- Modal form -->
  <div class="modal" id="modal">
    <div class="modal-card" role="dialog" aria-modal="true" aria-labelledby="modalTitle">
      <h2 id="modalTitle">ابعت رسالة حب</h2>
      <div class="form-row">
        <input id="toName" placeholder="اسم اللي هتبعتله (اختياري)" />
      </div>
      <div class="form-row">
        <textarea id="messageInput" placeholder="اكتب الرسالة هنا...">بحبك يا قلبي، كل يوم بيفضل قلبي يدعيلك وأتمنى تكوني سعيدة.</textarea>
      </div>
      <div class="modal-actions">
        <button class="btn" id="closeModal">إلغاء</button>
        <button class="btn primary" id="sendBtn">ابعت</button>
      </div>
      <p class="small" style="margin-top:10px">ملاحظة: الآن الزر مجرد محاكاة — تقدر توصل الفورم لـ EmailJS لو حابب يروح لإيميل حقيقي.</p>
    </div>
  </div>

  <script src="script.js"></script>
</body>
</html>
